import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.valenciaApp.app',
  appName: 'mosaic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
